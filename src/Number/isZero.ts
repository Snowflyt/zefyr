/**
 * Returns `true` if the number is zero.
 *
 * @example
 * ```typescript
 * isZero(1.95); // => false
 * isZero(0); // => true
 * isZero(-0); // => true
 * ```
 */
const isZero = (n: number): boolean => n === 0;

export default isZero;
